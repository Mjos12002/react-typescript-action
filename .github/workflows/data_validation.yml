name: data-validation
on: push
jobs:
  data-validation:
    runs-on: ubuntu-latest
    steps:
      - name: initial-test
        run: echo "Initial Testing Now"

      - name: buil-app
        run: npm build

      - name: test-app
        name: npm test